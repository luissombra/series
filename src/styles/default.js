import { StyleSheet } from 'react-native'

const styles = {
    row: {
        flexDirection: 'row'
    },
    centered: {
        alignContent: 'center',
        justifyContent: 'center',
        textAlign: 'center'
    },
    marginBottom: {
        marginBottom: 10
    }
}

export default StyleSheet.create(styles)